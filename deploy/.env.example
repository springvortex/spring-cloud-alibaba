# 镜像 tag；默认与 Maven 工程版本一致
APP_TAG=1.0.0

# dev 激活镜像内置开发基线；/app/config/application.yaml 外置配置覆盖基础设施地址
SPRING_PROFILE=dev

# Jasypt 主密钥：只在部署机填写，用于解密 ENC(...) 密文，绝不提交 Git
JASYPT_ENCRYPTOR_PASSWORD=change-me
