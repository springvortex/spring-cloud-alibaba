server:
  port: 80

spring:
  application:
    name: service-gateway
  profiles:
    active: dev
    include:
      - zipkin
      - sentinel
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
      config:
        enabled: false
