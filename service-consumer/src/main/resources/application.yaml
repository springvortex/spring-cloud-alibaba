server:
  port: 9002

spring:
  application:
    name: service-consumer
  profiles:
    active: dev
    include:
      - api
      - jasypt
      - zipkin
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
      config:
        enabled: false
