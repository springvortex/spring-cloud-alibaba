server:
  port: 9004

spring:
  application:
    name: service-mail
  profiles:
    active: ${zjc.config.env},${zjc.config.source}
    group:
      local:
        - api
        - jasypt
        - zipkin
