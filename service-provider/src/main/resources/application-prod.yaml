server:
  port: 9001

spring:
  application:
    name: service-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spring_cloud_alibaba?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: spring_cloud_alibaba
    password: ENC(vStS7aXxtuaitgTfFqkF4DevOB1cYe4pkfNFJCdIWSo=)

management:
  tracing:
    export:
      zipkin:
        endpoint: http://127.0.0.1:9411/api/v2/spans

mybatis-plus:
  mapper-locations: classpath:/mapper/**.xml
  type-aliases-package: com.zjc.provider
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id
      logic-delete-field: isDeleted

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
