spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        username: nacos
        password: nacos
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
  config:
    import:
      - nacos:prod?group=service-provider&namespace=public&refreshEnabled=true
      - nacos:pub?group=service-provider&namespace=public&refreshEnabled=true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.100.128:3306/spring_cloud_alibaba_prod?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: spring_cloud_alibaba
    password: fEmTS3Cfz3DTpMMS

mybatis-plus:
  mapper-locations: classpath:/mapper/**.xml
  type-aliases-package: com.zjc.provider
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id
      logic-delete-field: isDeleted

# 生产环境关闭接口文档，避免暴露 API 结构
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
