server:
  port: 9001

spring:
  application:
    name: service-provider
  profiles:
    active: dev
    include:
      - api
      - jasypt
      - zipkin
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
      config:
        enabled: false
